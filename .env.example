# ── Model ─────────────────────────────────────────────────────────────────
# Ollama must be running locally (http://localhost:11434)
MODEL=qwen2.5:14b

AT_RISK_THRESHOLD=75.0

# ── Schema / Domain Config ────────────────────────────────────────────────
# Configure these to point the agent at any tabular SQL schema
PRIMARY_TABLE=your_table_name
METRIC_COLUMN=status
POSITIVE_VALUE=active
DATE_COLUMN=date
ENTITY_COLUMN=entity_id
ENTITY_NAME_COLUMN=entity_name
GROUP_COLUMNS=

# ── Spark ─────────────────────────────────────────────────────────────────
# local[*] uses all CPU cores. For a remote cluster: spark://host:7077
SPARK_MASTER=local[*]
SPARK_APP_NAME=pyspark-ai-agent
# Root directory containing Parquet or CSV data files
DATA_PATH=./data

# ── RAG / Vector Store ────────────────────────────────────────────────────
# Path to the persistent ChromaDB directory (relative to project root)
CHROMA_PATH=.chroma
# Ollama embedding model — must be pulled first: ollama pull nomic-embed-text
EMBED_MODEL=nomic-embed-text
# Directory scanned for policy docs (.pdf and .md)
DOCS_PATH=docs

# ── MCP Server ────────────────────────────────────────────────────────────
MCP_USER_ID=mcp_user

# ── Web API ───────────────────────────────────────────────────────────────
# Secret key for JWT signing — change this in production
JWT_SECRET=change-me-in-production
# Default admin password (used only on first startup if api/users.json is empty)
ADMIN_PASSWORD=admin123

# ── Prompt Validation ─────────────────────────────────────────────────────
# Maximum length (chars) of a single chat message
MAX_MESSAGE_LEN=2000
# Maximum estimated tokens (message + conversation history) before rejection
MAX_PROMPT_TOKENS=2048
