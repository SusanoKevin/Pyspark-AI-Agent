# PySpark AI Agent runs Spark in local mode — no Docker service required.
# Data is loaded from Parquet/CSV files in the DATA_PATH directory.
# To generate test data: python scripts/seed_data.py
services: {}
